---
title: "BOJ 8595 - 히든 넘버"
date: "2024-02-18"
description: "문자열, 파싱 문제"
tags: ["문자열", "파싱"]
platform: "boj"
problem_number: "8595"
problem_url: "https://www.acmicpc.net/problem/8595"
difficulty: "브론즈 I"
language: "java"
---

## 문제

[BOJ 8595 - 히든 넘버](https://www.acmicpc.net/problem/8595)

## 풀이

문자열 처리를 통해 문제를 해결한다.

## 코드

```java
package day749;

import java.io.*;

public class Day747BOJ8595히든넘버 {

  public static void main(String[] args) throws Exception {
    BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    final String N = br.readLine();
    final String S = br.readLine();

    Long ans = 0L;
    StringBuilder sb = new StringBuilder();

    for (char c : S.toCharArray()) {
      if (Character.isDigit(c)) {
        sb.append(c);
      } else if (sb.length() > 0) {
        ans += Long.parseLong(sb.toString());
        sb.setLength(0);
      }
    }

    if (sb.length() > 0) {
      ans += Long.parseLong(sb.toString());
    }
    System.out.println(ans);
  }
}
```

## 복잡도

- 시간: O(N)
- 공간: O(N)
