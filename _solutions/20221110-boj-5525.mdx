---
title: "BOJ 5525 - IOIOI"
date: "2022-11-10"
description: "문자열 문제"
tags: ["문자열"]
platform: "boj"
problem_number: "5525"
problem_url: "https://www.acmicpc.net/problem/5525"
difficulty: "실버 I"
language: "java"
---

## 문제

[BOJ 5525 - IOIOI](https://www.acmicpc.net/problem/5525)

## 풀이

문자열 처리를 통해 문제를 해결한다.

## 코드

```java
package ASP_study.day299;

import java.io.*;

public class Day276BOJ5525IOIOI {
	public static void main(String[] args) throws Exception {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

		int N = Integer.parseInt(br.readLine());
		int M = Integer.parseInt(br.readLine());
		char s[] = br.readLine().toCharArray();

		int result = 0;
		int count = 0;

		for (int i = 1; i < M - 1; i++) {
			if (s[i - 1] == 'I' && s[i] == 'O' && s[i + 1] == 'I') {
				count++;

				if (count == N) {
					count--;
					result++;
				}
				i++;
			} else {
				count = 0;
			}
		}

		System.out.println(result);
		br.close();
	}
}
```

## 복잡도

- 시간: O(N)
- 공간: O(N)
