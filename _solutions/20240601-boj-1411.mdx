---
title: "BOJ 1411 - 비슷한 단어"
date: "2024-06-01"
description: "문자 대응 패턴이 같은 단어 쌍의 수를 구하는 문제"
tags: ["구현", "문자열", "브루트포스"]
platform: "boj"
problem_number: "1411"
problem_url: "https://www.acmicpc.net/problem/1411"
difficulty: "실버 II"
language: "python"
---

## 문제

[BOJ 1411 - 비슷한 단어](https://www.acmicpc.net/problem/1411)

> N개의 단어 중 문자 대응 패턴(구조)이 동일한 쌍의 개수를 구한다.

## 풀이

각 단어의 문자를 등장 순서에 따라 숫자로 치환하여 정규화된 패턴을 만든다. 예를 들어 "aba"는 "121"이 된다. 이후 모든 쌍을 비교하여 패턴이 동일한 경우를 센다.

## 코드

```python
import sys

input = sys.stdin.readline

n = int(input())
lst = []
for _ in range(n):
    a = list(input().strip())
    d = {}
    tmp = 1
    for j in range(len(a)):
        if a[j] not in d:
            d[a[j]] = tmp
            a[j] = d[a[j]]
            tmp += 1
        else:
            a[j] = d[a[j]]
    lst.append("".join(map(str, a)))

ans = 0

for i in range(n - 1):
    for j in range(i + 1, n):
        if lst[i] == lst[j]:
            ans += 1

print(ans)
```

## 복잡도

- 시간: O(N^2 * L) (L은 단어 길이)
- 공간: O(N * L)
