---
title: "BOJ 1639 - 행운의 티켓"
date: "2024-06-17"
description: "앞뒤 절반의 자릿수 합이 같은 가장 긴 부분 문자열을 구하는 문제"
tags: ["구현", "브루트포스"]
platform: "boj"
problem_number: "1639"
problem_url: "https://www.acmicpc.net/problem/1639"
difficulty: "실버 IV"
language: "python"
---

## 문제

[BOJ 1639 - 행운의 티켓](https://www.acmicpc.net/problem/1639)

> 숫자 문자열에서 앞쪽 절반과 뒤쪽 절반의 자릿수 합이 같은 가장 긴 짝수 길이 부분 문자열의 길이를 구한다.

## 풀이

가능한 짝수 길이를 큰 것부터 탐색한다. 각 길이에 대해 모든 시작 위치에서 앞뒤 절반의 자릿수 합을 비교하여, 같으면 해당 길이를 반환한다. 길이를 2씩 줄여가며 가장 먼저 발견된 길이가 답이다.

## 코드

```python
import sys


def check(x):
    mid = len(x) // 2
    sum1 = sum(x[:mid])
    sum2 = sum(x[mid:])
    if sum1 == sum2:
        return True
    else:
        return False


def solution(arr):
    if len(arr) % 2 == 0:
        chk = len(arr)
    else:
        chk = len(arr) - 1
    while chk > 0:
        i = 0
        ans = 0
        while i + chk <= len(arr):
            if check(arr[i : i + chk]):
                ans = chk
                return ans
            i += 1
        chk -= 2
    return 0


s = list(map(int, list(sys.stdin.readline().strip())))
print(solution(s))
```

## 복잡도

- 시간: O(N^2)
- 공간: O(N)
