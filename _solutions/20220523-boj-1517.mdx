---
title: "BOJ 1517 - 버블 소트"
date: "2022-05-23"
description: "자료 구조, 정렬, 세그먼트 트리 문제"
tags: ["자료 구조", "정렬", "세그먼트 트리", "분할 정복"]
platform: "boj"
problem_number: "1517"
problem_url: "https://www.acmicpc.net/problem/1517"
difficulty: "플래티넘 V"
language: "java"
---

## 문제

[BOJ 1517 - 버블 소트](https://www.acmicpc.net/problem/1517)

## 풀이

정렬을 활용하여 문제를 해결한다. 분할 정복을 사용하여 문제를 나누어 해결한다.

## 코드

```java
package com.ssafy.an.day149;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Day105BOJ1517버블솔트머지솔트 { // 1517 제목은 버블솔트인데, 버블솔트 쓰면 안됨.
	static int N;
	static long ans;
	static long[] arr, tmp;

	public static void main(String[] args) throws Exception {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

		N = Integer.parseInt(br.readLine());
		arr = new long[N];
		tmp = new long[N];

		StringTokenizer st = new StringTokenizer(br.readLine());
		for (int i = 0; i < N; i++)
			arr[i] = Integer.parseInt(st.nextToken());

		divide(0, N - 1);
		System.out.println(ans);
		br.close();
	}

	static void divide(int st, int ed) {
		if (st < ed) {
			int mid = (st + ed) / 2;

			divide(st, mid);
			divide(mid + 1, ed);

			merge(st, mid, ed);
		}
	}

	static void merge(int st, int mid, int ed) {
		int i = st, j = mid + 1, idx = st;

		while (i < mid + 1 && j < ed + 1)
			if (arr[i] <= arr[j])
				tmp[idx++] = arr[i++];
			else {
				tmp[idx++] = arr[j++];
				ans += (mid + 1 - i);
			}

		while (i < mid + 1)
			tmp[idx++] = arr[i++];

		while (j < ed + 1)
			tmp[idx++] = arr[j++];

		for (int n = st; n < ed + 1; n++)
			arr[n] = tmp[n];

	}
}
```

## 복잡도

- 시간: O(N log N)
- 공간: O(N)
