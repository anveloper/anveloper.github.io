---
title: "BOJ 10813 - 공 바꾸기"
date: "2024-01-14"
description: "구현, 시뮬레이션 문제"
tags: ["구현", "시뮬레이션"]
platform: "boj"
problem_number: "10813"
problem_url: "https://www.acmicpc.net/problem/10813"
difficulty: "브론즈 II"
language: "java"
---

## 문제

[BOJ 10813 - 공 바꾸기](https://www.acmicpc.net/problem/10813)

## 풀이

문제의 조건을 그대로 구현한다.

## 코드

```java
package day749;

import java.util.*;
import java.io.*;

public class Day712BOJ10813공바꾸기 {
  public static void main(String[] args) throws Exception {
    BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    StringBuilder sb = new StringBuilder();
    StringTokenizer st;

    int N, M;

    st = new StringTokenizer(br.readLine());

    N = Integer.parseInt(st.nextToken());
    M = Integer.parseInt(st.nextToken());

    int[] bucket = new int[N + 1];

    for (int i = 1; i <= N; i++) {
      bucket[i] = i;
    }

    for (int i = 0; i < M; i++) {
      st = new StringTokenizer(br.readLine());
      int a = Integer.parseInt(st.nextToken());
      int b = Integer.parseInt(st.nextToken());

      int temp = bucket[a];
      bucket[a] = bucket[b];
      bucket[b] = temp;
    }

    for (int i = 1; i < N + 1; i++) {
      sb.append(bucket[i] + " ");
    }
    System.out.println(sb);
  }
}
```

## 복잡도

- 시간: O(N)
- 공간: O(N)
