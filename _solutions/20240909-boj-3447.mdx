---
title: "BOJ 3447 - 버그왕"
date: "2024-09-09"
description: "문자열에서 BUG를 반복 제거하는 파싱 문제"
tags: ["문자열", "파싱"]
platform: "boj"
problem_number: "3447"
problem_url: "https://www.acmicpc.net/problem/3447"
difficulty: "브론즈 I"
language: "cpp"
---

## 문제

[BOJ 3447 - 버그왕](https://www.acmicpc.net/problem/3447)

> 문자열에서 "BUG" 부분 문자열을 더 이상 없을 때까지 반복적으로 제거한다.

## 풀이

`regex_replace`를 사용하여 문자열에서 "BUG"를 빈 문자열로 치환한다. "BUG" 제거 후 새로운 "BUG"가 생길 수 있으므로 `find`로 "BUG"가 없을 때까지 반복한다. EOF까지 각 줄을 처리한다.

## 코드

```cpp
#include <bits/stdc++.h>
using namespace std;
string s;

int main()
{
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  while (getline(cin, s))
  {
    while (s.find("BUG") != -1)
    {
      s = regex_replace(s, regex("BUG"), "");
    }
    cout << s << "\n";
  }
}
```

## 복잡도

- 시간: O(N^2 / 3) (최악의 경우 중첩된 BUG 반복 제거)
- 공간: O(N) (N: 문자열 길이)
