---
title: "BOJ 11772 - POT"
date: "2024-11-26"
description: "각 수의 밑과 지수를 분리하여 거듭제곱의 합을 구하는 문제"
tags: ["수학", "사칙연산"]
platform: "boj"
problem_number: "11772"
problem_url: "https://www.acmicpc.net/problem/11772"
difficulty: "브론즈 III"
language: "cpp"
---

## 문제

[BOJ 11772 - POT](https://www.acmicpc.net/problem/11772)

> 각 수의 마지막 자리가 지수이고 나머지가 밑인 형태로 주어질 때, 모든 수의 거듭제곱 합을 구한다.

## 풀이

각 입력을 10으로 나눈 몫을 밑, 나머지를 지수로 사용한다. `pow(밑, 지수)`로 계산한 값을 모두 합산하여 출력한다.

## 코드

```cpp
#include <iostream>
#include <cmath>

using namespace std;

int main(void)
{
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  int N;
  cin >> N;
  long long sum = 0;
  for (int i = 0; i < N; i++)
  {
    int temp;
    cin >> temp;
    sum += pow(temp / 10, temp % 10);
  }
  cout << sum << "\n";
  return 0;
}
```

## 복잡도

- 시간: O(N) (N: 입력 수의 개수)
- 공간: O(1)
