---
title: "BOJ 2075 - N번째 큰 수"
date: "2022-12-02"
description: "자료 구조, 정렬, 우선순위 큐 문제"
tags: ["자료 구조", "정렬", "우선순위 큐"]
platform: "boj"
problem_number: "2075"
problem_url: "https://www.acmicpc.net/problem/2075"
difficulty: "실버 III"
language: "java"
---

## 문제

[BOJ 2075 - N번째 큰 수](https://www.acmicpc.net/problem/2075)

## 풀이

정렬을 활용하여 문제를 해결한다.

## 코드

```java
package ASP_study.day299;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.PriorityQueue;
import java.util.StringTokenizer;

public class Day298BOJ2075N번째큰수 {
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer st = null;

		int n = Integer.parseInt(br.readLine());

		PriorityQueue<Integer> pq = new PriorityQueue<>();

		st = new StringTokenizer(br.readLine());
		for (int j = 0; j < n; j++) {
			pq.add(Integer.parseInt(st.nextToken()));
		}

		for (int i = 1; i < n; i++) {
			st = new StringTokenizer(br.readLine());
			for (int j = 0; j < n; j++) {
				int num = Integer.parseInt(st.nextToken());

				if (pq.peek() < num) {
					pq.poll();
					pq.offer(num);
				}
			}
		}

		System.out.println(pq.peek());

	}
}
```

## 복잡도

- 시간: O(N log N)
- 공간: O(N)
