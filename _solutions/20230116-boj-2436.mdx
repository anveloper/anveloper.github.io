---
title: "BOJ 2436 - 공약수"
date: "2023-01-16"
description: "수학, 브루트포스 알고리즘, 정수론 문제"
tags: ["수학", "브루트포스 알고리즘", "정수론", "유클리드 호제법"]
platform: "boj"
problem_number: "2436"
problem_url: "https://www.acmicpc.net/problem/2436"
difficulty: "골드 V"
language: "java"
---

## 문제

[BOJ 2436 - 공약수](https://www.acmicpc.net/problem/2436)

## 풀이

수학적 성질을 이용하여 풀이한다.

## 코드

```java
package day349;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Day343BOJ2436공약수 {
    public static void main(String[] args) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        int gcd = Integer.parseInt(st.nextToken());
        int lcd = Integer.parseInt(st.nextToken());

        long mul = (long) gcd * lcd;

        int a = 0, b = 0;

        for (int i = gcd; i <= Math.sqrt(mul); i += gcd) {
            if (mul % i == 0 && euclidean(i, mul / i) == gcd) {
                a = i;
                b = (int) (mul / i);
            }
        }

        System.out.println(a + " " + b);
    }

    public static long euclidean(long a, long b) {
        long r = a % b;
        if (r == 0)
            return b;
        return euclidean(b, r);
    }
}
```

## 복잡도

- 시간: O(N)
- 공간: O(N)
