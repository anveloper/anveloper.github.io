---
title: "BOJ 6888 - Terms of Office"
date: "2025-11-15"
description: "수학, 정수론 문제"
tags: ["수학", "정수론"]
platform: "boj"
problem_number: "6888"
problem_url: "https://www.acmicpc.net/problem/6888"
difficulty: "브론즈 IV"
language: "cpp"
---

## 문제

[BOJ 6888 - Terms of Office](https://www.acmicpc.net/problem/6888)

## 풀이

수학적 성질을 이용하여 풀이한다.

## 코드

```cpp
#include <iostream>

using namespace std;

int main()
{
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int x, y;
  cin >> x >> y;

  for (int year = x; year <= y; year++)
  {
    if (!((year - x) % 4) && !((year - x) % 2) && !((year - x) % 3) && !((year - x) % 5))
      cout << "All positions change in year " << year << "\n";
  }

  return 0;
}
```

## 복잡도

- 시간: O(N)
- 공간: O(N)
