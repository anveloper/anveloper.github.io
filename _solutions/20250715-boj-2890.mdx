---
title: "BOJ 2890 - 카약"
date: "2025-07-15"
description: "구현, 문자열, 정렬 문제"
tags: ["구현", "문자열", "정렬"]
platform: "boj"
problem_number: "2890"
problem_url: "https://www.acmicpc.net/problem/2890"
difficulty: "실버 V"
language: "python"
---

## 문제

[BOJ 2890 - 카약](https://www.acmicpc.net/problem/2890)

## 풀이

정렬을 활용하여 문제를 해결한다.

## 코드

```python
import sys

input = sys.stdin.readline
r, c = map(int, input().rstrip().split())
maps = [list(input().rstrip()) for _ in range(r)]
arr = []
for i in range(r):
    for k in range(1, 10):
        if maps[i].count(str(k)) > 0:
            cnt = 0
            for j in range(c - 1, -1, -1):
                if maps[i][j] == str(k):
                    break
                cnt += 1
            arr.append([cnt, k, 0])
arr.sort(key=lambda x: x[0])
arr[0][2] = 1
now = 1
for i in range(1, len(arr)):
    if arr[i][0] != arr[i - 1][0]:
        now += 1
    arr[i][2] = now
arr.sort(key=lambda x: x[1])
for i in range(len(arr)):
    print(arr[i][2])
```

## 복잡도

- 시간: O(N log N)
- 공간: O(N)
