---
title: "BOJ 1356 - 유진수"
date: "2024-04-20"
description: "숫자를 두 부분으로 나눠 각 자릿수의 곱이 같은지 판별"
tags: ["수학", "구현", "브루트포스"]
platform: "boj"
problem_number: "1356"
problem_url: "https://www.acmicpc.net/problem/1356"
difficulty: "브론즈 I"
language: "javascript"
---

## 문제

[BOJ 1356 - 유진수](https://www.acmicpc.net/problem/1356)

> 주어진 숫자를 두 부분으로 나누었을 때, 각 부분의 자릿수 곱이 같으면 유진수이다.

## 풀이

모든 분할 위치에서 왼쪽과 오른쪽 자릿수의 곱을 비교한다.

## 코드

```javascript
const solution = (input) => {
  const str = input[0].split("").map(Number);
  for (let i = 1; i < str.length; i++) {
    const front = str.slice(0, i).reduce((acc, cur) => acc * cur, 1),
      back = str.slice(i).reduce((acc, cur) => acc * cur, 1);
    if (front === back) return "YES";
  }
  return "NO";
};
const fs = require("fs");
const input = fs.readFileSync("/dev/stdin").toString().trim().split("\n");
console.log(solution(input));
```

## 복잡도

- 시간: O(N²)
- 공간: O(N)
