---
title: "BOJ 6219 - 소수의 자격"
date: "2025-10-21"
description: "수학, 정수론, 소수 판정 문제"
tags: ["수학", "정수론", "소수 판정", "에라토스테네스의 체"]
platform: "boj"
problem_number: "6219"
problem_url: "https://www.acmicpc.net/problem/6219"
difficulty: "실버 III"
language: "cpp"
---

## 문제

[BOJ 6219 - 소수의 자격](https://www.acmicpc.net/problem/6219)

## 풀이

수학적 성질을 이용하여 풀이한다.

## 코드

```cpp
#include <iostream>
#include <vector>
#include <cmath>

const int MAX_NUM = 4000001;
using namespace std;

bool hasDigit(int number, int digit)
{
  while (number > 0)
  {
    if (number % 10 == digit)
    {
      return true;
    }
    number /= 10;
  }
  return false;
}

int main(void)
{
  ios::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int A, B, D;
  cin >> A >> B >> D;

  vector<int> Prime(MAX_NUM + 1, 1);
  Prime[0] = Prime[1] = 0;

  for (int i = 2; i <= sqrt(MAX_NUM); i++)
  {
    for (int j = i * i; j <= MAX_NUM; j += i)
    {
      Prime[j] = 0;
    }
  }

  int count = 0;
  for (int i = A; i <= B; i++)
  {
    if (Prime[i] == 1 && hasDigit(i, D))
    {
      count++;
    }
  }
  cout << count << '\n';
  return 0;
}
```

## 복잡도

- 시간: O(N log log N)
- 공간: O(N)
