---
title: "BOJ 5532 - 방학 숙제"
date: "2024-09-16"
description: "방학 일수에서 숙제 완료에 필요한 최소 일수를 빼는 문제"
tags: ["수학", "사칙연산"]
platform: "boj"
problem_number: "5532"
problem_url: "https://www.acmicpc.net/problem/5532"
difficulty: "브론즈 IV"
language: "cpp"
---

## 문제

[BOJ 5532 - 방학 숙제](https://www.acmicpc.net/problem/5532)

> 방학 일수 L, 국어 숙제 A페이지(하루 C페이지), 수학 숙제 B페이지(하루 D페이지)가 주어질 때, 숙제를 끝내고 놀 수 있는 일수를 구한다.

## 풀이

국어와 수학 숙제 각각의 완료 일수를 올림 나눗셈으로 계산하고, 둘 중 더 오래 걸리는 쪽을 방학 일수에서 빼면 놀 수 있는 날이 된다.

## 코드

```cpp
#include <iostream>
#include <algorithm>

using namespace std;

int main(void)
{
  int l, a, b, c, d, ans;

  cin >> l >> a >> b >> c >> d;
  if (a % c == 0)
    ans = a / c;
  else
    ans = a / c + 1;

  if (b % d == 0)
    ans = max(ans, b / d);
  else
    ans = max(ans, b / d + 1);

  cout << l - ans << endl;
  return 0;
}
```

## 복잡도

- 시간: O(1)
- 공간: O(1)
