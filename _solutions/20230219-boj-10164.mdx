---
title: "BOJ 10164 - 격자상의 경로"
date: "2023-02-19"
description: "수학, 다이나믹 프로그래밍, 조합론 문제"
tags: ["수학", "다이나믹 프로그래밍", "조합론"]
platform: "boj"
problem_number: "10164"
problem_url: "https://www.acmicpc.net/problem/10164"
difficulty: "실버 II"
language: "java"
---

## 문제

[BOJ 10164 - 격자상의 경로](https://www.acmicpc.net/problem/10164)

## 풀이

다이나믹 프로그래밍을 사용하여 부분 문제의 해를 메모이제이션하며 풀이한다.

## 코드

```java
package day399;

import java.io.*;
import java.util.*;

public class Day377BOJ10164격자상의경로 {
  static int N, M, K;
  static int[][] p;

  public static void main(String[] args) throws Exception {
    BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    StringTokenizer st = new StringTokenizer(br.readLine());

    N = Integer.parseInt(st.nextToken());
    M = Integer.parseInt(st.nextToken());
    K = Integer.parseInt(st.nextToken());
    p = new int[N][M];

    for (int i = 0; i < N; i++) {
      for (int j = 0; j < M; j++) {
        if (i == 0 || j == 0)
          p[i][j] = 1;
        else
          p[i][j] = p[i - 1][j] + p[i][j - 1];
      }
    }

    if (K != 0) {
      int x = (K - 1) / M;
      int y = (K - 1) % M;
      System.out.println(p[x][y] * p[N - x - 1][M - y - 1]);
    } else {
      System.out.println(p[N - 1][M - 1]);
    }
  }
}
```

## 복잡도

- 시간: O(N²)
- 공간: O(N)
