---
title: "BOJ 4159 - 알래스카"
date: "2025-08-16"
description: "정렬 문제"
tags: ["정렬"]
platform: "boj"
problem_number: "4159"
problem_url: "https://www.acmicpc.net/problem/4159"
difficulty: "실버 III"
language: "cpp"
---

## 문제

[BOJ 4159 - 알래스카](https://www.acmicpc.net/problem/4159)

## 풀이

정렬을 활용하여 문제를 해결한다.

## 코드

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
  int input, n;
  int answer;
  vector<int> v;

  while (1)
  {
    answer = 1;
    cin >> input;
    if (input == 0)
      break;

    for (int i = 0; i < input; i++)
    {
      cin >> n;
      v.push_back(n);
    }

    sort(v.begin(), v.end());

    for (int i = 1; i < v.size(); i++)
    {
      if (v[i] - v[i - 1] > 200)
      {
        answer = 0;
        break;
      }
    }

    if ((1422 - v[v.size() - 1]) * 2 > 200)
    {
      answer = 0;
    }

    if (answer == 1)
      cout << "POSSIBLE" << endl;
    else
      cout << "IMPOSSIBLE" << endl;
    v.clear();
  }

  return 0;
}
```

## 복잡도

- 시간: O(N log N)
- 공간: O(N)
