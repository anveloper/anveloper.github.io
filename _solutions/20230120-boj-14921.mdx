---
title: "BOJ 14921 - 용액 합성하기"
date: "2023-01-20"
description: "두 포인터 문제"
tags: ["두 포인터"]
platform: "boj"
problem_number: "14921"
problem_url: "https://www.acmicpc.net/problem/14921"
difficulty: "골드 V"
language: "java"
---

## 문제

[BOJ 14921 - 용액 합성하기](https://www.acmicpc.net/problem/14921)

## 풀이

투 포인터 기법을 활용한다.

## 코드

```java
package day349;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Day347BOJ14921용액합성하기 {
    static int N, s, e, ans = Integer.MAX_VALUE;
    static int[] arr;

    public static void main(String[] args) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        N = Integer.parseInt(br.readLine());
        arr = new int[N];

        StringTokenizer st = new StringTokenizer(br.readLine());
        for (int i = 0; i < N; i++)
            arr[i] = Integer.parseInt(st.nextToken());

        s = 0;
        e = N - 1;
        while (s < e) {
            int tmp = arr[s] + arr[e];
            ans = Math.abs(tmp) < Math.abs(ans) ? tmp : ans;

            if (tmp == 0)
                break;
            else if (tmp < 0)
                s++;
            else if (tmp > 0)
                e--;
        }

        System.out.println(ans);
        br.close();
    }
}
```

## 복잡도

- 시간: O(N)
- 공간: O(N)
