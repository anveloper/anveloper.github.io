---
title: "BOJ 1377 - 버블 소트"
date: "2023-07-18"
description: "정렬 문제"
tags: ["정렬"]
platform: "boj"
problem_number: "1377"
problem_url: "https://www.acmicpc.net/problem/1377"
difficulty: "골드 II"
language: "java"
---

## 문제

[BOJ 1377 - 버블 소트](https://www.acmicpc.net/problem/1377)

## 풀이

정렬을 활용하여 문제를 해결한다.

## 코드

```java
package day549;

import java.io.*;
import java.util.*;

public class Day527BOJ1377버블소트 {
  static class Point implements Comparable<Point> {
    int num;
    int idx;

    Point(int num, int idx) {
      this.num = num;
      this.idx = idx;
    }

    @Override
    public int compareTo(Point o) {
      return num - o.num;
    }
  }

  public static void main(String[] args) throws Exception {
    BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
    int N = Integer.parseInt(br.readLine());

    Point[] points = new Point[N + 1];
    for (int i = 1; i <= N; i++) {
      int temp = Integer.parseInt(br.readLine());
      points[i] = new Point(temp, i);
    }
    Arrays.sort(points, 1, N + 1);

    int max = 0;
    for (int i = 1; i <= N; i++) {
      max = Math.max(max, points[i].idx - i);
    }

    bw.write((max + 1) + "\n");
    bw.close();
    br.close();
  }
}
```

## 복잡도

- 시간: O(N log N)
- 공간: O(N)
