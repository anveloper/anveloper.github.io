---
title: "BOJ 4150 - 피보나치 수"
date: "2024-08-18"
description: "큰 수 연산을 이용하여 N번째 피보나치 수를 구하는 문제"
tags: ["수학", "큰 수 연산"]
platform: "boj"
problem_number: "4150"
problem_url: "https://www.acmicpc.net/problem/4150"
difficulty: "브론즈 I"
language: "cpp"
---

## 문제

[BOJ 4150 - 피보나치 수](https://www.acmicpc.net/problem/4150)

> N이 주어졌을 때 N번째 피보나치 수를 출력한다. N이 최대 10000이므로 매우 큰 수가 될 수 있다.

## 풀이

N이 최대 10000이면 피보나치 수가 일반 정수형 범위를 초과하므로 문자열 기반 큰 수 덧셈을 구현한다. 두 문자열을 뒤집어 자릿수별로 더하고 올림(carry)을 처리한 뒤 다시 뒤집어 결과를 얻는다. 반복적으로 `a`, `b`를 갱신하며 N번째 피보나치 수를 계산한다.

## 코드

```cpp
#include <iostream>
#include <cstring>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;

string sum(string x, string y)
{
  int num;
  int carry = 0;
  string result;

  reverse(x.begin(), x.end());
  reverse(y.begin(), y.end());

  while (x.length() < y.length())
  {
    x += '0';
  }
  while (x.length() > y.length())
  {
    y += '0';
  }

  for (int i = 0; i < x.length(); ++i)
  {
    num = (x[i] - '0' + y[i] - '0' + carry) % 10;
    result += to_string(num);
    carry = (x[i] - '0' + y[i] - '0' + carry) / 10;
  }
  if (carry != 0)
  {
    result += to_string(carry);
  }

  reverse(result.begin(), result.end());

  return result;
}

int main(int argc, char *argv[])
{
  int n;
  string a;
  string b;
  string result;

  cin >> n;

  a = '0';
  b = '1';

  if (n == 0)
  {
    result = "0";
  }
  if (n == 1)
  {
    result = "1";
  }

  for (int i = 2; i <= n; ++i)
  {
    result = sum(a, b);
    a = b;
    b = result;
  }

  cout << result << endl;

  return 0;
}
```

## 복잡도

- 시간: O(N * D) (D: 피보나치 수의 자릿수, 최대 약 2000자리)
- 공간: O(D)
