---
title: "BOJ 1235 - 학생 번호"
date: "2024-03-02"
description: "구현, 자료 구조, 문자열 문제"
tags: ["구현", "자료 구조", "문자열", "브루트포스 알고리즘", "집합과 맵"]
platform: "boj"
problem_number: "1235"
problem_url: "https://www.acmicpc.net/problem/1235"
difficulty: "실버 IV"
language: "java"
---

## 문제

[BOJ 1235 - 학생 번호](https://www.acmicpc.net/problem/1235)

## 풀이

문제의 조건을 그대로 구현한다.

## 코드

```java
package day799;

import java.io.*;
import java.util.*;

public class Day760BOJ1235학생번호 {
  public static void main(String[] args) throws IOException {
    BufferedReader in = new BufferedReader(new InputStreamReader(System.in));
    int N = Integer.parseInt(in.readLine());
    Set<String> set = new HashSet<>();

    String[] input = new String[N];
    for (int i = 0; i < N; i++)
      input[i] = in.readLine();
    int len = input[0].length();

    for (int k = 1; k <= len; k++) {
      for (int i = 0; i < N; i++) {
        set.add(input[i].substring(len - k));
      }
      if (set.size() == N) {
        System.out.println(k);
        return;
      }
      set.clear();
    }
  }
}
```

## 복잡도

- 시간: O(N²)
- 공간: O(N)
