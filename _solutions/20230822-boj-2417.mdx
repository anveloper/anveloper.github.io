---
title: "BOJ 2417 - 정수 제곱근"
date: "2023-08-22"
description: "수학, 이분 탐색 문제"
tags: ["수학", "이분 탐색"]
platform: "boj"
problem_number: "2417"
problem_url: "https://www.acmicpc.net/problem/2417"
difficulty: "실버 IV"
language: "java"
---

## 문제

[BOJ 2417 - 정수 제곱근](https://www.acmicpc.net/problem/2417)

## 풀이

이분 탐색을 통해 최적의 값을 탐색한다.

## 코드

```java
package day599;

import java.io.*;

public class Day562BOJ2417정수제곱근 {
  public static void main(String[] args) throws Exception {
    BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

    long n = Long.parseLong(br.readLine());
    System.out.println(bSearch(n));
  }

  static long bSearch(long n) {
    long start = 0;
    long end = n;
    long result = 0;

    while (start <= end) {
      long mid = (start + end) / 2;
      if (n <= (long) Math.pow(mid, 2)) {
        result = mid;
        end = mid - 1;
      } else {
        start = mid + 1;
      }
    }
    return result;
  }
}
```

## 복잡도

- 시간: O(N log X)
- 공간: O(N)
