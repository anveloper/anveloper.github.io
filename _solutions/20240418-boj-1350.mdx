---
title: "BOJ 1350 - 진짜 공간"
date: "2024-04-18"
description: "파일 크기를 클러스터 단위로 올림하여 실제 차지하는 공간을 계산"
tags: ["수학", "구현"]
platform: "boj"
problem_number: "1350"
problem_url: "https://www.acmicpc.net/problem/1350"
difficulty: "브론즈 II"
language: "javascript"
---

## 문제

[BOJ 1350 - 진짜 공간](https://www.acmicpc.net/problem/1350)

> 파일들의 크기가 주어질 때, 클러스터 단위로 올림하여 실제 디스크에서 차지하는 공간의 합을 구한다.

## 풀이

각 파일 크기를 클러스터 크기로 나눈 뒤 올림하여 합산한다.

## 코드

```javascript
const solution = (input) => {
  const N = +input[0];
  const arr = input[1].split(" ").map(Number);
  const C = +input[2];
  return arr.reduce((p, c) => p + Math.ceil(c / C), 0) * C;
};
const fs = require("fs");
const input = fs.readFileSync("/dev/stdin").toString().trim().split("\n");
console.log(solution(input));
```

## 복잡도

- 시간: O(N)
- 공간: O(1)
