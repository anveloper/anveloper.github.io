---
title: "BOJ 1198 - 삼각형으로 자르기"
date: "2024-02-27"
description: "수학, 브루트포스 알고리즘, 기하학 문제"
tags: ["수학", "브루트포스 알고리즘", "기하학"]
platform: "boj"
problem_number: "1198"
problem_url: "https://www.acmicpc.net/problem/1198"
difficulty: "실버 II"
language: "java"
---

## 문제

[BOJ 1198 - 삼각형으로 자르기](https://www.acmicpc.net/problem/1198)

## 풀이

수학적 성질을 이용하여 풀이한다.

## 코드

```java
package day799;

import java.io.*;

public class Day756BOJ1198삼각형으로자르기 {

  public static void main(String[] args) throws Exception {
    BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));

    int n = Integer.parseInt(br.readLine());
    int[][] points = new int[n][2];
    for (int i = 0; i < n; i++) {
      String[] coordinate = br.readLine().split(" ");
      points[i][0] = Integer.parseInt(coordinate[0]);
      points[i][1] = Integer.parseInt(coordinate[1]);
    }

    double max = 0;
    for (int a = 0; a < n - 2; a++) {
      for (int b = a + 1; b < n - 1; b++) {
        for (int c = b + 1; c < n; c++) {
          max = Math.max(max, area(points[a], points[b], points[c]));
        }
      }
    }

    bw.write(Double.toString(max));
    br.close();
    bw.close();
  }

  private static double area(int[] a, int[] b, int[] c) {
    return (double) Math.abs(a[0] * b[1] + b[0] * c[1] + c[0] * a[1] - a[1] * b[0] - b[1] * c[0] - c[1] * a[0]) / 2;
  }
}
```

## 복잡도

- 시간: O(N²)
- 공간: O(N)
