---
title: "BOJ 1229 - 육각수"
date: "2024-04-03"
description: "주어진 수를 최소 개수의 육각수 합으로 표현하는 문제"
tags: ["수학", "DP"]
platform: "boj"
problem_number: "1229"
problem_url: "https://www.acmicpc.net/problem/1229"
difficulty: "골드 IV"
language: "javascript"
---

## 문제

[BOJ 1229 - 육각수](https://www.acmicpc.net/problem/1229)

> 자연수 N을 육각수(1, 6, 15, 28, ...)의 합으로 나타낼 때 필요한 최소 항의 개수를 구한다.

## 풀이

i번째 육각수 공식 i(2i-1)로 육각수 목록을 미리 계산한다. DP 배열에서 dp[i]는 i를 만드는 데 필요한 최소 육각수 개수이며, 각 육각수를 빼가며 최솟값을 갱신한다.

## 코드

```javascript
const solution = (input) => {
  const N = +input;
  const nums = [];
  const dp = Array(N + 1).fill(6);
  for (let i = 1; i * (2 * i - 1) <= 1_000_000; i++) nums[i] = i * (2 * i - 1);
  dp[0] = 0;
  dp[1] = 1;
  for (let i = 2; i <= N; i++) {
    for (let j = 1; nums[j] <= i; j++) {
      if (dp[i - nums[j]] + 1 < dp[i]) dp[i] = dp[i - nums[j]] + 1;
    }
  }
  return dp[N];
};

const fs = require("fs");
const input = fs.readFileSync("/dev/stdin").toString().trim().split("\n");
console.log(solution(input));
```

## 복잡도

- 시간: O(N × √N)
- 공간: O(N)
