---
title: "BOJ 14490 - 백대열"
date: "2022-11-04"
description: "수학, 문자열, 정수론 문제"
tags: ["수학", "문자열", "정수론", "유클리드 호제법"]
platform: "boj"
problem_number: "14490"
problem_url: "https://www.acmicpc.net/problem/14490"
difficulty: "실버 V"
language: "java"
---

## 문제

[BOJ 14490 - 백대열](https://www.acmicpc.net/problem/14490)

## 풀이

수학적 성질을 이용하여 풀이한다.

## 코드

```java
package ASP_study.day299;

import java.util.Scanner;

public class Day270BOJ14490백대열 {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		String[] str = sc.nextLine().split(":");
		int N = Integer.parseInt(str[0]);
		int M = Integer.parseInt(str[1]);
		int GCD = gcd(Math.max(N, M), Math.min(N, M));

		System.out.println(N / GCD + ":" + M / GCD);
		sc.close();
	}

	static int gcd(int a, int b) {
		if (b == 0)
			return a;
		return gcd(b, a % b);
	}
}
```

## 복잡도

- 시간: O(log N)
- 공간: O(1)
