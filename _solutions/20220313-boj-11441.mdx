---
title: "BOJ 11441 - 합 구하기"
date: "2022-03-13"
description: "누적 합 문제"
tags: ["누적 합"]
platform: "boj"
problem_number: "11441"
problem_url: "https://www.acmicpc.net/problem/11441"
difficulty: "실버 III"
language: "java"
---

## 문제

[BOJ 11441 - 합 구하기](https://www.acmicpc.net/problem/11441)

## 풀이

누적 합을 활용하여 구간 합을 효율적으로 계산한다.

## 코드

```java
package com.ssafy.an.day049;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Day31BOJ11441합구하기부분합쓰면좋음 { // 11441 합 구하기
	public static void main(String[] args) throws Exception {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringBuilder sb = new StringBuilder();
		StringTokenizer st = null;

		int N = Integer.parseInt(br.readLine());
		st = new StringTokenizer(br.readLine());
		int[] arr = new int[N + 1];
		int tmp = 0;
		int idx = 0;
		while (st.hasMoreTokens()) {
			tmp += Integer.parseInt(st.nextToken());
			arr[++idx] = tmp;
		}
		int M = Integer.parseInt(br.readLine());
		for (int n = 0; n < M; n++) {
			st = new StringTokenizer(br.readLine());
			int i = Integer.parseInt(st.nextToken());
			int j = Integer.parseInt(st.nextToken());

			sb.append(arr[j] - arr[i - 1]).append("\n");
		}
		System.out.println(sb);
		br.close();
	}
}
```

## 복잡도

- 시간: O(N)
- 공간: O(N)
