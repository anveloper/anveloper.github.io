---
title: "BOJ 1132 - 합"
date: "2024-03-17"
description: "그리디 알고리즘 문제"
tags: ["그리디 알고리즘"]
platform: "boj"
problem_number: "1132"
problem_url: "https://www.acmicpc.net/problem/1132"
difficulty: "골드 III"
language: "java"
---

## 문제

[BOJ 1132 - 합](https://www.acmicpc.net/problem/1132)

## 풀이

그리디 알고리즘으로 매 단계 최선의 선택을 한다.

## 코드

```java
package day799;

import java.io.*;
import java.util.*;

public class Day775BOJ1132합 {

  public static void main(String[] args) throws Exception {
    BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    int n = Integer.parseInt(br.readLine());
    long[] values = new long[10];
    boolean[] flag = new boolean[10];
    for (int i = 0; i < n; i++) {
      String input = br.readLine();

      long pos = 1;
      for (int j = input.length() - 1; j >= 0; j--) {
        if (j == 0)
          flag[input.charAt(j) - 'A'] = true;
        values[input.charAt(j) - 'A'] += pos;
        pos *= 10;
      }

    }

    int find_idx = 0;
    long min_val = Long.MAX_VALUE;
    for (int i = 0; i < values.length; i++) {
      if (!flag[i] && min_val > values[i]) {
        find_idx = i;
        min_val = values[i];
      }
    }

    values[find_idx] = 0;

    Arrays.sort(values);

    long ans = 0;
    int pos = 9;

    for (int i = values.length - 1; i >= 0; i--) {
      if (values[i] == 0)
        break;
      ans += values[i] * pos;
      pos--;
    }
    ans += pos * min_val;

    System.out.println(ans);
  }
}
```

## 복잡도

- 시간: O(N log N)
- 공간: O(N)
