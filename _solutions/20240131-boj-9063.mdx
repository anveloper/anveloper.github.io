---
title: "BOJ 9063 - 대지"
date: "2024-01-31"
description: "수학, 구현, 기하학 문제"
tags: ["수학", "구현", "기하학"]
platform: "boj"
problem_number: "9063"
problem_url: "https://www.acmicpc.net/problem/9063"
difficulty: "브론즈 III"
language: "java"
---

## 문제

[BOJ 9063 - 대지](https://www.acmicpc.net/problem/9063)

## 풀이

문제의 조건을 그대로 구현한다.

## 코드

```java
package day749;

import java.io.*;
import java.util.*;

public class Day729BOJ9063대지 {
  public static void main(String[] args) throws Exception {
    BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    int N = Integer.parseInt(br.readLine());
    int[] A = new int[N];
    int[] B = new int[N];

    for (int i = 0; i < N; i++) {
      StringTokenizer st = new StringTokenizer(br.readLine());
      A[i] = Integer.parseInt(st.nextToken());
      B[i] = Integer.parseInt(st.nextToken());
    }
    Arrays.sort(A);
    Arrays.sort(B);
    System.out.println((A[N - 1] - A[0]) * (B[N - 1] - B[0]));
  }
}
```

## 복잡도

- 시간: O(N log N)
- 공간: O(N)
