---
title: "BOJ 3059 - 등장하지 않는 문자의 합"
date: "2024-08-19"
description: "대문자 A~Z 중 문자열에 없는 문자들의 ASCII 합을 구하는 문제"
tags: ["구현", "문자열"]
platform: "boj"
problem_number: "3059"
problem_url: "https://www.acmicpc.net/problem/3059"
difficulty: "브론즈 III"
language: "cpp"
---

## 문제

[BOJ 3059 - 등장하지 않는 문자의 합](https://www.acmicpc.net/problem/3059)

> 대문자로 이루어진 문자열이 주어졌을 때, A~Z 중 등장하지 않는 문자들의 ASCII 값 합을 구한다.

## 풀이

배열을 이용해 문자열에 등장하는 문자를 체크한 뒤, A(65)부터 Z(90)까지 순회하며 등장하지 않은 문자의 ASCII 값을 합산한다.

## 코드

```cpp
#include <bits/stdc++.h>
using namespace std;
int T, ch[100], ans;
string s;
void solve()
{
  ans = 0;
  memset(ch, 0, sizeof(ch));
  for (int i : s)
    ch[i] = 1;
  for (int i = 65; i <= 90; i++)
    if (!ch[i])
      ans += i;
}
int main()
{
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  cin >> T;
  while (T--)
  {
    cin >> s;
    solve();
    cout << ans << "\n";
  }
  return 0;
}
```

## 복잡도

- 시간: O(T * (N + 26)) (N: 문자열 길이)
- 공간: O(1)
