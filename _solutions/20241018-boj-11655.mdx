---
title: "BOJ 11655 - ROT13"
date: "2024-10-18"
description: "문자열에 ROT13 암호화를 적용하여 출력하는 문제"
tags: ["구현", "문자열"]
platform: "boj"
problem_number: "11655"
problem_url: "https://www.acmicpc.net/problem/11655"
difficulty: "브론즈 I"
language: "cpp"
---

## 문제

[BOJ 11655 - ROT13](https://www.acmicpc.net/problem/11655)

> 알파벳을 13자리씩 밀어서 암호화하는 ROT13을 적용한 결과를 출력한다.

## 풀이

문자열의 각 문자를 순회하며, 소문자이면 13을 더한 뒤 'z'를 초과하면 26을 빼고, 대문자이면 같은 방식으로 처리한다. 알파벳이 아닌 문자는 그대로 출력한다.

## 코드

```cpp
#include <iostream>
#include <string>
using namespace std;

int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  string arr;
  getline(cin, arr);
  for (int i = 0; i < arr.length(); i++)
  {
    if (arr[i] >= 'a' && arr[i] <= 'z')
    {
      arr[i] += 13;
      if (arr[i] > 'z' || arr[i] < 'a')
      {
        arr[i] -= 26;
      }
    }
    else if (arr[i] >= 'A' && arr[i] <= 'Z')
    {
      arr[i] += 13;
      if (arr[i] > 'Z')
      {
        arr[i] -= 26;
      }
    }

    cout << arr[i];
  }
}
```

## 복잡도

- 시간: O(L) (L: 문자열 길이)
- 공간: O(L)
