---
title: "BOJ 11648 - 지속"
date: "2024-10-19"
description: "각 자릿수를 곱하는 과정을 반복하여 한 자리가 될 때까지의 횟수를 구하는 문제"
tags: ["수학", "구현"]
platform: "boj"
problem_number: "11648"
problem_url: "https://www.acmicpc.net/problem/11648"
difficulty: "브론즈 III"
language: "cpp"
---

## 문제

[BOJ 11648 - 지속](https://www.acmicpc.net/problem/11648)

> 주어진 수의 각 자릿수를 곱하는 연산을 한 자리 수가 될 때까지 반복하고, 반복 횟수를 출력한다.

## 풀이

숫자를 문자열로 관리하면서 각 자릿수의 곱을 계산한다. 곱이 0이면 즉시 "0"이 되고, 그렇지 않으면 곱을 다시 문자열로 변환한다. 한 자리가 될 때까지 반복하여 단계 수를 센다.

## 코드

```cpp
#include <bits/stdc++.h>
using namespace std;

string s;
int stage;

int getNum(string str)
{
  int num = 1;
  if (str == "0")
    return 0;
  for (auto s : str)
    num *= s - '0';
  return num;
}

int main()
{
  cin >> s;

  while (1)
  {
    if (s.size() == 1)
      break;
    stage++;
    int num = getNum(s);
    s = to_string(num);
  }

  cout << stage;
}
```

## 복잡도

- 시간: O(d * k) (d: 자릿수, k: 반복 횟수)
- 공간: O(d)
